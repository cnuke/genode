--- src/linux/kernel/time/tick-sched.c
+++ src/linux/kernel/time/tick-sched.c
@@ -54,7 +54,7 @@
 /*
  * Must be called with interrupts disabled !
  */
-static void tick_do_update_jiffies64(ktime_t now)
+void tick_do_update_jiffies64(ktime_t now)
 {
 	unsigned long ticks = 1;
 	ktime_t delta, nextp;
