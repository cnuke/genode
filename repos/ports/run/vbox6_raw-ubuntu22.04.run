set ::env(LOG) core
set ::env(SCULPT) vbox6_raw-ubuntu22.04

source ${genode_dir}/repos/gems/run/sculpt.run

if { [get_cmd_switch --autopilot] } {
run_genode_until {\[init -> runtime\] child "vbox6_raw.*" exited with exit value 0} 90
} else {
run_genode_until forever
}
